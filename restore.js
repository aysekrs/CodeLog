const fs = require('fs'); const files = ['backend/src/main/java/com/nazli/blog/config/KategoriBaslangicCalistirici.java', 'backend/src/main/java/com/nazli/blog/config/KategoriBaslangicServisi.java']; for(let file of files) { let str = fs.readFileSync(file, 'utf8'); if (str.charCodeAt(0) === 0xFEFF) str = str.substring(1); let buf = Buffer.from(str, 'utf16le'); fs.writeFileSync(file, buf); }

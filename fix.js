const fs = require('fs'); const path = require('path'); function walk(dir) { fs.readdirSync(dir).forEach(file => { const fullPath = path.join(dir, file); if (fs.statSync(fullPath).isDirectory()) { walk(fullPath); } else if (fullPath.endsWith('.java')) { let content = fs.readFileSync(fullPath); let text = content.toString('utf8'); if (text.includes('\u0000')) { text = content.toString('utf16le'); } text = text.replace(/^\uFEFF/, ''); fs.writeFileSync(fullPath, text, 'utf8'); } }); } walk('backend/src/main/java');
